# 训练进度显示优化

**问题**: 训练时长时间没有任何输出,用户不知道是否卡住

**原因**:
- 每局对弈需要1-2分钟
- 每10局才显示一次进度
- 前10-15分钟都没有任何输出!

---

## 解决方案

### 修改前 (trainer.py)
```python
for i in range(num_games):
    game_data, winner = self_play_game(...)

    # 每10局显示一次进度
    if (i + 1) % 10 == 0:
        print(f"进度: {i+1}/{num_games}")
```

**问题**: 前10局(约10-15分钟)都没有输出,用户以为程序卡住了!

---

### 修改后
```python
for i in range(num_games):
    # 开始前显示
    print(f"对弈 {i+1}/{num_games}...", end='', flush=True)

    game_data, winner = self_play_game(...)

    # 立即显示结果
    result = "红胜" if winner == 1 else "黑胜" if winner == -1 else "和局"
    print(f" {result} ({len(game_data)}步)")

    # 每10局显示统计
    if (i + 1) % 10 == 0:
        print(f"进度: {i+1}/{num_games} | 红:{red_wins} 黑:{black_wins} 和:{draws}")
```

**效果**: 每局都实时显示!

---

## 输出效果对比

### 修改前:
```
▶ 阶段1: 自我对弈 (100局)...
(等待10-15分钟...没有任何输出)
   进度: 10/100 | ...
```
**用户**: "卡住了?要不要Ctrl+C?"

### 修改后:
```
▶ 阶段1: 自我对弈 (100局)...
   对弈 1/100... 和局 (200步)
   对弈 2/100... 红胜 (87步)
   对弈 3/100... 黑胜 (95步)
   ...
   进度: 10/100 | 红:4 黑:3 和:3 | 平均步数: 120.5
```
**用户**: "很好,正在训练,进度清晰!"

---

## 测试结果

运行 `python test_training_quick.py` (3局快速测试):

```
============================================================
快速训练测试(3局)
============================================================
已加载模型,已训练 5 局

开始测试训练流程...
   对弈 1/3... 和局 (200步)
   对弈 2/3... 红胜 (87步)
   对弈 3/3... 黑胜 (95步)

✓ 训练流程正常!
  完成3局对弈
  缓冲区大小: 382
```

---

## 性能测试结果

运行 `python test_performance.py`:

```
合法走法生成速度:
  平均每次: 3.1毫秒  ✓ 正常

单局对弈速度:
  平均每步: 1.61秒   ✓ 正常
  预计一局: 1.6s × 50步 = 80秒
  预计100局: 约2-3小时
```

**结论**: 速度正常,不是性能问题,只是缺少进度显示!

---

## 其他优化

### 1. 确保日志目录存在
```python
def evaluate(self):
    os.makedirs(LOG_DIR, exist_ok=True)  # 新增
    log_file = f"{LOG_DIR}/training.log"
    ...
```

**修复**: `plot_progress.py` 报错"日志文件不存在"

---

### 2. 添加平均步数统计
```python
total_moves = 0

for i in range(num_games):
    ...
    total_moves += len(game_data)

    if (i + 1) % 10 == 0:
        avg_moves = total_moves / (i + 1)
        print(f"平均步数: {avg_moves:.1f}")
```

**效果**: 可以看到AI是否在进步(步数减少=学会快速获胜)

---

## 训练时间估算

基于性能测试结果:

| 配置 | 每步耗时 | 每局步数 | 每局耗时 | 100局耗时 |
|------|---------|---------|---------|----------|
| 当前(MCTS=50) | 1.6秒 | 50-200步 | 1-5分钟 | 2-3小时 |
| 快速(MCTS=25) | 0.8秒 | 50-200步 | 0.5-3分钟 | 1-2小时 |
| 高质量(MCTS=200) | 6秒 | 50-200步 | 5-20分钟 | 10-15小时 |

**建议**: 初次训练用MCTS=25快速看效果,确认正常后再用MCTS=50或更高

---

## 使用方法

### 完整训练
```bash
python main.py train
```

输出示例:
```
============================================================
开始训练中国象棋AI
============================================================
设备: cuda
每轮自我对弈: 100局
MCTS模拟次数: 50
缓冲区大小: 10000
============================================================

【第 1/100 轮训练】
时间: 2025-11-26 12:05:42

▶ 阶段1: 自我对弈 (100局)...
   对弈 1/100... 和局 (200步)
   对弈 2/100... 红胜 (87步)
   对弈 3/100... 黑胜 (95步)
   ...
   进度: 10/100 | 红:4 黑:3 和:3 | 平均步数: 120.5
   ...
   ✓ 完成 100 局对弈

▶ 阶段2: 训练神经网络...
   平均损失: 0.3542

进度: 总对局数=100, 缓冲区=5000
------------------------------------------------------------
```

---

## 中断和恢复

### 中断训练
按 **Ctrl+C** 即可,模型会自动保存

### 恢复训练
```bash
python main.py train
```
自动加载 `models/latest.pt` 继续训练

---

## 常见问题

**Q: 为什么第一局这么慢?**
A: 初始化神经网络需要时间,之后会稳定在1-2分钟/局

**Q: 100局需要多久?**
A: MCTS=50时约2-3小时,可以在config.py中调小MCTS_SIMULATIONS加速

**Q: 如何看进步曲线?**
A: 训练5轮后运行 `python plot_progress.py`

**Q: 可以在后台训练吗?**
A: 可以,Windows下用 `start /b python main.py train > train.log`

---

## 总结

✅ **问题已解决**: 每局都有实时进度显示
✅ **性能正常**: 1.6秒/步,2-3小时/100局
✅ **日志完整**: logs/training.log 自动创建
✅ **可中断恢复**: 随时Ctrl+C,下次继续

现在可以放心训练了! 🚀

---

**日期**: 2025-11-26
**文件**: trainer.py, test_training_quick.py, test_performance.py
